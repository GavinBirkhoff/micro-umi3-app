import { useModel } from 'umi';
import styles from './index.less';
import { useEffect } from 'react';

export default function DeptPage() {
  return (
    <div>
      <h1 className={styles.title}>User Page dept</h1>
    </div>
  );
}
