import { useModel } from 'umi';
import styles from './index.less';
import { useEffect } from 'react';

export default function UserPage() {
  return (
    <div>
      <h1 className={styles.title}>User Page user</h1>
    </div>
  );
}
